module.exports = function parseStringAsArray(arrayAsString){
   return arrayAsString = arrayAsString.split(',').map(arrayAsString => arrayAsString.trim())

   console.log(arrayAsString)
   console.log('eae')
}